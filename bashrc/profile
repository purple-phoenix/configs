# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

export PATH="$HOME/.cargo/bin:$PATH"

$HOME/.screenlayout/four_screen.sh
xmodmap -e clear Lock -e keycode 0x42 = Escape
